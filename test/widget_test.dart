import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('NoteFlow smoke test', (WidgetTester tester) async {
    // Firebase & Hive require platform initialization; run integration tests separately.
    expect(true, isTrue);
  });
}
